# ============================================
# 开发环境配置
# 启动命令：npm run dev
# ============================================
# ⚠️  Vite 环境变量必须以 VITE_ 开头才能在客户端代码中访问

# API 基础 URL（Vite 代理目标地址）
VITE_API_TARGET=http://localhost:8080

# WebSocket URL（相对路径，通过 Vite 代理访问）
VITE_WS_URL=/ws

# 应用标题
VITE_APP_TITLE=答题游戏 - 开发环境

# 开发服务器端口
VITE_DEV_PORT=5173

# 是否启用 Mock 数据（可选）
VITE_ENABLE_MOCK=false

# 开发环境特定配置
NODE_ENV=development
