# ============================================
# 环境变量配置示例
# 复制此文件为 .env.development 或 .env.production 并根据需要修改
# ============================================

# ============ 开发环境配置示例 ============
# API 基础 URL（Vite 代理目标地址，仅开发环境使用）
VITE_API_TARGET=http://localhost:8080

# WebSocket URL（相对路径）
VITE_WS_URL=/ws

# 应用标题
VITE_APP_TITLE=答题游戏

# 开发服务器端口
VITE_DEV_PORT=5173

# 是否启用 Mock 数据
VITE_ENABLE_MOCK=false

# ============ 生产环境配置示例 ============
# 生产环境只需要配置应用标题和 WebSocket URL
# API 请求通过 Nginx 反向代理，不需要 VITE_API_TARGET

# ============ 说明 ============
# 1. Vite 环境变量必须以 VITE_ 开头才能在客户端代码中访问
# 2. .env.development 用于开发环境（npm run dev）
# 3. .env.production 用于生产环境（npm run build）
# 4. 本地开发可以创建 .env.local 覆盖默认配置
# 5. 不要将 .env.local 和 .env.*.local 提交到版本控制
# 6. 只有 .env.example 应该被提交到 Git
