# ============================================
# 生产环境配置
# 构建命令：npm run build
# ============================================
# ⚠️  Vite 环境变量必须以 VITE_ 开头才能在客户端代码中访问

# WebSocket URL（生产环境使用相对路径，通过 Nginx 代理）
VITE_WS_URL=/ws

# 应用标题
VITE_APP_TITLE=答题游戏

# 生产环境特定配置
NODE_ENV=production

# ============================================
# 注意事项：
# ============================================
# 1. 生产环境的 API 请求通过 Nginx 反向代理到后端
#    因此不需要配置 VITE_API_TARGET
#
# 2. WebSocket 也通过 Nginx 代理：
#    location /ws {
#        proxy_pass http://localhost:8080/ws;
#        proxy_http_version 1.1;
#        proxy_set_header Upgrade $http_upgrade;
#        proxy_set_header Connection "upgrade";
#    }
#
# 3. 前端静态文件部署路径：
#    将 dist/ 目录内容部署到 Nginx root 目录
# ============================================
